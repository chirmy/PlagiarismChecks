<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.plagiarismchecks">

    <uses-permission android:name="android.permission.SET_WALLPAPER"/>
<!--    <uses-sdk tools:overrideLibrary="jp.wasabeef.glide.transformations"/>-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="智助教"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight">
        <activity android:name=".teacher.activity.HomeActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".teacher.activity.PublishTaskActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:parentActivityName=".teacher.activity.HomeActivity">


        </activity>
    </application>

</manifest>